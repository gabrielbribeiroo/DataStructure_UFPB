#ifndef MENU_H // checks if the header has already been included
#define MENU_H // if not, define the header

#include "create.h" // for vector, and linked list

// functions
void menu();
void display_menu_vector_list();
void display_menu_linked_list();

#endif // end of the header guard